import { reducer, initialState } from './buying-investment-distribution.reducer';

describe('Buying Investment Distribution Reducer', () => {
  describe('unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;

      const result = reducer(initialState, action);

      expect(result).toBe(initialState);
    });
  });
});
